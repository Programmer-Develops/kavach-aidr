# ============================================================
# KAVACH-AIDR — Requirements (All Phases)
# 100% open-source, 100% sovereign, zero foreign API calls
# ============================================================

# --- CLI & Display ---
rich>=13.7.0              # Beautiful terminal output, progress bars, tables
click>=8.1.7              # CLI argument parsing

# --- Static Analysis ---
bandit>=1.7.8             # Python security linter (AST-based)
semgrep>=1.50.0           # Pattern-based static analysis engine

# --- Local LLM Inference (no internet, no foreign API) ---
# Phi-3-mini (Microsoft) or CodeLlama (Meta) via llama.cpp
# For CPU-only:  pip install llama-cpp-python
# For CUDA GPU:  CMAKE_ARGS="-DLLAMA_CUDA=on" pip install llama-cpp-python
llama-cpp-python>=0.2.56

# --- Formal Verification ---
z3-solver>=4.12.4.0       # Microsoft Z3 SMT solver

# --- System Utilities ---
psutil>=5.9.8             # CPU / RAM / GPU detection

# --- Data & Utilities ---
python-dotenv>=1.0.1
PyYAML>=6.0.1
toml>=0.10.2

# --- Testing ---
pytest>=8.2.0
pytest-cov>=5.0.0

# --- Phase 3: Web Dashboard ---
streamlit>=1.35.0         # Web UI dashboard
plotly>=5.22.0            # Interactive charts
pandas>=2.2.0             # Data manipulation for dashboard

# --- API Server (optional — for future REST integration) ---
fastapi>=0.111.0
uvicorn>=0.30.0
pydantic>=2.7.0
